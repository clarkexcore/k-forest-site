import React, { Fragment } from 'react';

import Navigation from '../layouts/Navigation';

const AltHeader = () => (
    <Fragment>
        <Navigation />
    </Fragment>
);

export default AltHeader;